server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaowenwy/sprincloud-config.git
          search-paths:
            - springcloud-config
          default-label: main
      label: main
      request-connect-timeout: 20000
      request-read-timeout: 20000
  mvc:
    async:
      request-timeout: 20000
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka



